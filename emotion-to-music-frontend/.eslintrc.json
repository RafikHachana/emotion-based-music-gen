{
  "extends": "next/core-web-vitals",
  "rules": {
    // Additional, per-project rules...
    // tab size
    // max line length
    "max-len": ["error", { "code": 120 }],
    "indent": ["error", 4]
  }
}
